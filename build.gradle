// Root build.gradle
buildscript {
    repositories {
        google()        // <--- This is required for AGP
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.2.2" // Match SDK version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
